NODE_ENV=development

#서버 구성
SERVER_PORT=3000
HOST=localhost
HOST_URL=http://localhost:8080

#sql 서버 구성
SQL_USER=sa
SQL_PASSWORD=rkskekfk1!
SQL_DATABASE=edu_and_test
SQL_SERVER=192.168.45.63
SQL_PORT=59305
SQL_ENCRYPT=false

# user 테이블
# create table user(name varchar(100) primary key not null, id varchar(100), pw varchar(100))
# post 테이블
# create table post(id int identity(1, 1)  primary key not null, name varchar(100) foreign key references users(name),title varchar(100), content varchar(255), la_time datetime, views int)